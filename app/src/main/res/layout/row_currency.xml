<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
>

    <data>
        <variable
            name="rate"
            type="com.artf.fixer.domain.Rate"/>

        <variable
            name="clickListener"
            type="com.artf.fixer.listView.ListViewPagingAdapter.OnClickListener"/>
    </data>

    <LinearLayout android:id="@+id/row"
                  android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:layout_margin="8dp"
                  android:foreground="?android:attr/selectableItemBackground"
                  android:focusable="true"
                  android:onClick="@{(v) -> clickListener.onClick(v, rate)}"
                  android:background="@drawable/shape_currency_row"
                  android:orientation="horizontal">



        <TextView
            android:id="@+id/symbolTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{rate.symbol}"
            android:gravity="center"
            android:lines="1"
            android:padding="10dp"
            android:textSize="16sp"/>


        <TextView
            android:id="@+id/rateTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:background="@drawable/shape_rate"
            android:text="@{rate.rate}"
            android:gravity="center|center_vertical"
            android:lines="1"
            android:padding="10dp"
            android:textSize="16sp"/>

    </LinearLayout>
</layout>